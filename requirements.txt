# Core Django and REST Framework
Django==4.2.11
djangorestframework==3.15.1

# Database - PostgreSQL adapter
psycopg2-binary==2.9.9

# Environment variables management
python-dotenv==1.0.1

# PDF Generation
WeasyPrint==61.2

# Excel/CSV Import & Export
django-import-export==3.3.9
openpyxl==3.1.2

# API filtering and pagination
django-filter==24.2

# CORS support (if frontend is separate)
django-cors-headers==4.3.1

# Token authentication (optional but included)
djangorestframework-simplejwt==5.3.1

# Background tasks (optional – for future scalability)
celery==5.3.6
redis==5.0.1

# Image & file handling (for document uploads)
Pillow==10.2.0

# Logging and debugging (optional but useful)
django-debug-toolbar==4.3.0

# Production server (Gunicorn for deployment)
gunicorn==21.2.0

